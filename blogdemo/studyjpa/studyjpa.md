가장 먼저 설정하는 application.yml 파일

콘솔 접속을 위한 설정을 해볼까
```yaml
spring:
  h2:
    console:
    enabled: true
    path: /h2
  datasource:
    url: jdbc:h2:mem:test
```

## 엔티티 설계
1. `post` 엔티티 
2. `comment` 엔티티
3. `User` 엔티티

User (1) ----< (N)  Post -----< (N) comment

한 명의 유저가 여러개의 post를 쓸 수있다.   
post 하나에는 여러개의 comment가 있을 수 있다.

## 연관관계 - 주인은 누구?
연관관계 매핑의 주인이란 무엇인가?   
연관관계 주인이 필요한 이유 : 객체 지향의 패러다임과 데이터베이스의 패러다임에 차이가 있기 때문이다.

> 양방향과 단방향 매핑   
> `양방향` : 두 객체 모두가 각각 참조용 필드를 갖고 참조한다.   
> `단방향` : 두 객체 사이에 하나의 객체만 참조용 필드를 가지고 참조한다.


연관관계의 주인(mappedBy)

연관관계의 주인을 지정한다는 것은, 객체의 두 관계 중 제어의 권한 (데이터 조회, 저장, 수정, 삭제)를 
갖는 실질적인 관계가 무엇인지 JPA에 알리는 것

따라서 연관관계의 주인은 연관관계를 갖는 두 객체 사이에서 조회, 저장, 수정, 삭제를 할 수 있지만,
주인이 아니면 조회만 가능하다.

누구를 주인으로 지정해줘야 하는가?

간단하게 주인은 외래키가 있는 곳을 주인으로 지정하면 된다.

프로젝트에 적용해보자. 유저와 게시글의 관계를 생각해보자. 유저가 게시글의 id 값을 가지고 있지 않을까? 아니지
게시글이 유저를 참조하고 있지 않을까? 

ERD에서 1대 N 관계를 설계할 때, N측 엔티티가 1측 엔티티의 ID를 참조하는 방식으로 관계를 맺는다.   
